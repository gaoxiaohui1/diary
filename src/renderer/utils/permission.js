// import router from '../router'
// import store from '../store'

// router.beforeEach((to, from, next) => {
//   if (store.getters.currentUser.ID === -1) { // 当前登陆人信息为空则跳转至登陆页面
//     next({ path: '/login' })
//   } else {
//     next()
//   }
// })
